spring.application.name=demo

# Include our validation messages (e.g. "averageGrade must be between 0 and 20") in error
# responses so the frontend can show them. Off by default since Spring Boot 2.3.
server.error.include-message=always
